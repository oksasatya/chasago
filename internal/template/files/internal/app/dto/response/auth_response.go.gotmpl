package response

// Auth-related response DTOs intentionally have no token payload here.
// Authentication is HttpOnly cookie-based — Login and Refresh write tokens
// into Set-Cookie headers and respond with an empty data envelope. See
// docs/boilerplate-fixes.md §7.
