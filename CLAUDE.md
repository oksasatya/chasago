# CLAUDE.md

<!-- autoskills:start -->

Summary generated by `autoskills`. Check the full files inside `.claude/skills`.

## Go Benchmarking & Performance Measurement

Golang benchmarking, profiling, and performance measurement. Use when writing, running, or comparing Go benchmarks, profiling hot paths with pprof, interpreting CPU/memory/trace profiles, analyzing results with benchstat, setting up CI benchmark regression detection, or investigating production p...

- `.claude/skills/golang-benchmark/SKILL.md`
- `.claude/skills/golang-benchmark/references/benchstat.md`: Each input is a file containing `go test -bench` output. Optionally label inputs with `label=path` syntax.
- `.claude/skills/golang-benchmark/references/ci-regression.md`: Runs Go benchmarks on two git refs and uses `benchstat` to display deltas. Caches results for non-worktree refs so re-runs are fast. Prevents macOS sleep during benchmarks.
- `.claude/skills/golang-benchmark/references/compiler-analysis.md`: The Go compiler provides diagnostic flags that reveal optimization decisions — escape analysis, inlining, SSA intermediate representation, and generated assembly. These are essential for understanding **why** a function allocates or **why** the compiler won't inline it.
- `.claude/skills/golang-benchmark/references/investigation-session.md`: Tools and techniques for **temporary deep-dive performance investigation** — not everyday monitoring. These are things you enable for hours or days while debugging a specific issue, then disable.
- `.claude/skills/golang-benchmark/references/pprof.md`: Each profile type answers a different performance question. Choosing the wrong profile type wastes investigation time — match the symptom to the profile before capturing.
- `.claude/skills/golang-benchmark/references/prometheus-go-metrics.md`: Complete listing of Go runtime metrics **actually exposed as Prometheus metrics** by `prometheus/client_golang` library.
- `.claude/skills/golang-benchmark/references/tools.md`: Use these tools to validate the root cause of a slowdown BEFORE applying any optimization. Do NOT use auto-fix flags (e.g. `--fix`) — let the coding agent interpret results and apply changes manually with explanatory comments.
- `.claude/skills/golang-benchmark/references/trace.md`: Use the execution tracer when:

## Go CLI Best Practices

Golang CLI application development. Use when building, modifying, or reviewing a Go CLI tool — especially for command structure, flag handling, configuration layering, version embedding, exit codes, I/O patterns, signal handling, shell completion, argument validation, and CLI unit testing. Also t...

- `.claude/skills/golang-cli/SKILL.md`

## Go Code Style

Golang code style, formatting and conventions. Use when writing code, reviewing style, configuring linters, writing comments, or establishing project standards.

- `.claude/skills/golang-code-style/SKILL.md`
- `.claude/skills/golang-code-style/references/details.md`: When `if` conditions span multiple operands, extract into named booleans:

## Go Concurrency Best Practices

Golang concurrency patterns. Use when writing or reviewing concurrent Go code involving goroutines, channels, select, locks, sync primitives, errgroup, singleflight, worker pools, or fan-out/fan-in pipelines. Also triggers when you detect goroutine leaks, race conditions, channel ownership issues...

- `.claude/skills/golang-concurrency/SKILL.md`
- `.claude/skills/golang-concurrency/references/channels-and-select.md`: NEVER start a goroutine without knowing how it stops. Every goroutine MUST answer: **how will it stop?**
- `.claude/skills/golang-concurrency/references/pipelines.md`: A pipeline is a series of stages connected by channels, where each stage is a goroutine (or group of goroutines) that:
- `.claude/skills/golang-concurrency/references/sync-primitives.md`: Protects shared state with exclusive access. MUST hold the lock for the shortest time possible — NEVER hold a mutex across I/O, network calls, or channel operations.

## Go context.Context Best Practices

Idiomatic context.Context usage in Golang — creation, propagation, cancellation, timeouts, deadlines, context values, and cross-service tracing. Apply when working with context.Context in any Go code.

- `.claude/skills/golang-context/SKILL.md`
- `.claude/skills/golang-context/references/cancellation.md`: Every `WithCancel`, `WithTimeout`, and `WithDeadline` allocates internal resources (timers, goroutines). cancel() MUST be called (via defer) to prevent resource leaks. Even if the context will expire on its own, always defer cancel.
- `.claude/skills/golang-context/references/http-services.md`: Middleware injects request-scoped values before handlers run. Use unexported key types to prevent collisions:
- `.claude/skills/golang-context/references/values-tracing.md`: Context values carry request-scoped metadata that crosses API boundaries — not function parameters, configuration, or optional arguments. Good candidates: trace IDs, span IDs, request IDs, authenticated user info, correlation IDs.

## Go Database Best Practices

Comprehensive guide for Go database access. Covers parameterized queries, struct scanning, NULLable column handling, error patterns, transactions, isolation levels, SELECT FOR UPDATE, connection pool, batch processing, context propagation, and migration tooling. Use this skill whenever writing, r...

- `.claude/skills/golang-database/SKILL.md`
- `.claude/skills/golang-database/references/performance.md`: Check pool stats in production to detect exhaustion:
- `.claude/skills/golang-database/references/scanning.md`: Tag struct fields with `db:"column_name"` for sqlx:
- `.claude/skills/golang-database/references/testing.md`: Define a repository interface so business logic can be tested without a database. Mock the interface with `testify/mock`:
- `.claude/skills/golang-database/references/transactions.md`: Use `FOR UPDATE` when you read a value, compute something from it, and then write it back. Without the lock, concurrent transactions can read stale data.

## Go Design Patterns & Idioms

Idiomatic Golang design patterns — functional options, constructors, error flow and cascading, resource management and lifecycle, graceful shutdown, resilience, architecture, dependency injection, data handling, and streaming. Apply when designing Go APIs, structuring applications, choosing betwe...

- `.claude/skills/golang-design-patterns/SKILL.md`
- `.claude/skills/golang-design-patterns/references/architecture.md`: Architecture complexity MUST match project scope — don't over-architect small projects. When starting a new project, ask the developer what architecture they prefer:
- `.claude/skills/golang-design-patterns/references/clean-architecture.md`: Apply clean architecture when you need strong separation between business logic and infrastructure — typically medium-to-large services (2K+ lines) where testability, framework independence, and clear dependency direction matter. Do NOT use for small CLI tools or scripts.
- `.claude/skills/golang-design-patterns/references/data-handling.md`: Process large datasets without allocating everything into memory:
- `.claude/skills/golang-design-patterns/references/ddd.md`: Apply DDD when the business domain is complex enough that the code structure should mirror the business model — typically services with 5K+ lines, multiple bounded contexts, or rich business rules. Do NOT use for simple CRUD apps or CLI tools.
- `.claude/skills/golang-design-patterns/references/hexagonal-architecture.md`: Apply hexagonal architecture when a service interacts with multiple external systems (databases, APIs, message queues, caches) and you want the domain logic fully decoupled from all of them. Particularly effective when the same business logic needs multiple entry points (HTTP, gRPC, CLI, message...
- `.claude/skills/golang-design-patterns/references/resource-management.md`: This applies to all closeable resources: files, SQL rows, HTTP response bodies, gzip readers, bufio scanners wrapping readers, etc.

## Go Documentation

Comprehensive documentation guide for Golang projects, covering godoc comments, README, CONTRIBUTING, CHANGELOG, Go Playground, Example tests, API docs, and llms.txt. Use when writing or reviewing doc comments, documentation, adding code examples, setting up doc sites, or discussing documentation...

- `.claude/skills/golang-documentation/SKILL.md`
- `.claude/skills/golang-documentation/assets/templates/CHANGELOG.md`: All notable changes to this project will be documented in this file.
- `.claude/skills/golang-documentation/assets/templates/CONTRIBUTING.md`: Thank you for your interest in contributing!
- `.claude/skills/golang-documentation/assets/templates/README.md`: <!-- Replace {owner} and {repo} throughout this file -->
- `.claude/skills/golang-documentation/references/application.md`: → See `samber/cc-skills-golang@golang-cli` skill for CLI application patterns and frameworks.
- `.claude/skills/golang-documentation/references/code-comments.md`: → See `samber/cc-skills-golang@golang-naming` skill for naming conventions that reduce the need for comments.
- `.claude/skills/golang-documentation/references/library.md`: → See `samber/cc-skills-golang@golang-testing` skill for writing effective Example test functions.
- `.claude/skills/golang-documentation/references/project-docs.md`: → See `samber/cc-skills-golang@golang-continuous-integration` skill for automating changelog generation and release workflows.

## Go Code Modernization Guide

Continuously modernize Golang code to use the latest language features, standard library improvements, and idiomatic patterns. Use this skill whenever writing, reviewing, or refactoring Go code to ensure it leverages modern Go idioms. Also use when the user asks about Go upgrades, migration, mode...

- `.claude/skills/golang-modernize/SKILL.md`
- `.claude/skills/golang-modernize/references/tooling.md`: Beyond the Go language itself, suggest updating all non-functional developer tools that improve code quality or security for free. These are low-risk, high-value improvements: CI actions/plugins, linters (e.g. golangci-lint), SAST tools (e.g. gosec, Snyk, Semgrep), vulnerability scanners (e.g. go...
- `.claude/skills/golang-modernize/references/versions.md`: Changelog: <https://go.dev/doc/go1.21>

## Go Naming Conventions

Go (Golang) naming conventions — covers packages, constructors, structs, interfaces, constants, enums, errors, booleans, receivers, getters/setters, functional options, acronyms, test functions, and subtest names. Use this skill when writing new Go code, reviewing or refactoring, choosing between...

- `.claude/skills/golang-naming/SKILL.md`
- `.claude/skills/golang-naming/references/functions-methods.md`: Functions returning a value are named like **nouns** (what they return). Functions performing actions are named like **verbs** (what they do).
- `.claude/skills/golang-naming/references/identifiers.md`: Name length SHOULD be **proportional to scope size**. Short names for small scopes, descriptive names for large scopes.
- `.claude/skills/golang-naming/references/packages-files.md`: Package names MUST be **lowercase, single-word**, with no underscores or MixedCaps. They should be short, concise, and evocative of their purpose. Numbers are allowed (`oauth2`, `k8s`).
- `.claude/skills/golang-naming/references/testing.md`: Test functions follow `Test` + the name of what is being tested. Use underscores for subcases.
- `.claude/skills/golang-naming/references/types-errors.md`: Name them with the **method name + `-er`** suffix:

## Go Observability Best Practices

Golang everyday observability — the always-on signals in production. Covers structured logging with slog, Prometheus metrics, OpenTelemetry distributed tracing, continuous profiling with pprof/Pyroscope, server-side RUM event tracking, alerting, and Grafana dashboards. Apply when instrumenting Go...

- `.claude/skills/golang-observability/SKILL.md`
- `.claude/skills/golang-observability/references/alerting.md`: Alert on what matters to users. Google's SRE book defines four golden signals — every Go service SHOULD have alerts covering all four:
- `.claude/skills/golang-observability/references/dashboards.md`: Install these community Grafana dashboards to monitor Go runtime performance out of the box. They visualize the metrics automatically exposed by `github.com/prometheus/client_golang` — no custom instrumentation needed.
- `.claude/skills/golang-observability/references/logging.md`: → See `samber/cc-skills-golang@golang-error-handling` skill for the single handling rule.
- `.claude/skills/golang-observability/references/metrics.md`: → See `samber/cc-skills-golang@golang-troubleshooting` skill for using metrics to diagnose production issues. → See `samber/cc-skills@promql-cli` skill for executing and testing PromQL queries via CLI.
- `.claude/skills/golang-observability/references/profiling.md`: → See `samber/cc-skills-golang@golang-troubleshooting` skill (pprof.md) for on-demand debugging.
- `.claude/skills/golang-observability/references/rum.md`: Backend observability (logs, metrics, traces, profiles) tells you how your **system** behaves. RUM tells you how your **users** experience it. While frontend SDKs capture browser-side signals, the Go backend plays a critical role: tracking server-side business events, feeding Customer Data Platfo...
- `.claude/skills/golang-observability/references/tracing.md`: → See `samber/cc-skills-golang@golang-context` skill for propagating context across service boundaries. → See `samber/cc-skills-golang@golang-samber-oops` skill for structured errors with stack traces in spans.

## Go Development Patterns

Idiomatic Go patterns, best practices, and conventions for building robust, efficient, and maintainable Go applications.

- `.claude/skills/golang-patterns/SKILL.md`

## Go Performance Optimization

Golang performance optimization patterns and methodology - if X bottleneck, then apply Y. Covers allocation reduction, CPU efficiency, memory layout, GC tuning, pooling, caching, and hot-path optimization. Use when profiling or benchmarks have identified a bottleneck and you need the right optimi...

- `.claude/skills/golang-performance/SKILL.md`
- `.claude/skills/golang-performance/references/caching.md`: The fastest code is code that doesn't run. Caching pre-computed results, deduplicating concurrent requests, and avoiding unnecessary work are often the highest-leverage performance improvements.
- `.claude/skills/golang-performance/references/cpu.md`: CPU-bound bottlenecks show up as functions dominating the CPU profile. The patterns below target the most common causes: missed inlining opportunities, poor cache utilization, and unnecessary computation.
- `.claude/skills/golang-performance/references/io-networking.md`: Network and I/O bottlenecks show up as goroutines blocked on syscalls or waiting for responses. The key levers are connection reuse, proper timeouts, and streaming instead of buffering.
- `.claude/skills/golang-performance/references/memory.md`: Allocation reduction is the single highest-ROI optimization in most Go programs. Every allocation eventually requires garbage collection — reducing allocation count and size directly reduces GC pauses and CPU overhead.
- `.claude/skills/golang-performance/references/observability.md`: Third-party monitoring tools complement local profiling (pprof, benchmarks) by providing continuous monitoring, historical trends, and regression detection in production.
- `.claude/skills/golang-performance/references/runtime.md`: Runtime settings control garbage collection frequency, memory limits, CPU scheduling, and compiler optimizations. Tune them after profiling — the defaults are well-chosen for most workloads.

## Go Safety: Correctness & Defensive Coding

Defensive Golang coding to prevent panics, silent data corruption, and subtle runtime bugs. Use whenever writing or reviewing Go code that involves nil-prone types (pointers, interfaces, maps, slices, channels), numeric conversions, resource lifecycle (defer in loops), or defensive copying. Also...

- `.claude/skills/golang-safety/SKILL.md`
- `.claude/skills/golang-safety/references/nil-safety.md`: MUST check for nil before calling methods on pointer receivers from external sources. A method call on a nil pointer does not always panic — it depends on whether the method dereferences the receiver:
- `.claude/skills/golang-safety/references/slice-map-safety.md`: NEVER store pointers to loop variables in Go < 1.22 — capture by value. Before Go 1.22, the range loop variable was reused across iterations. Capturing it in a closure or storing its address caused all references to point to the final value:

## Go Security

Security best practices and vulnerability prevention for Golang. Covers injection (SQL, command, XSS), cryptography, filesystem safety, network security, cookies, secrets management, memory safety, and logging. Apply when writing, reviewing, or auditing Go code for security, or when working on an...

- `.claude/skills/golang-security/SKILL.md`
- `.claude/skills/golang-security/references/architecture.md`: Defense-in-depth, Zero Trust, and authentication patterns for Go services.
- `.claude/skills/golang-security/references/checklist.md`: Severity: Critical, High, Medium, Low
- `.claude/skills/golang-security/references/cookies.md`: Cookie security is critical for preventing session hijacking and XSS exploitation.
- `.claude/skills/golang-security/references/cryptography.md`: Cryptography vulnerabilities threaten confidentiality and integrity of sensitive data.
- `.claude/skills/golang-security/references/filesystem.md`: Filesystem vulnerabilities can lead to unauthorized file access, data leakage, and denial-of-service attacks.
- `.claude/skills/golang-security/references/injection.md`: Injection vulnerabilities allow attackers to execute arbitrary code, queries, or commands.
- `.claude/skills/golang-security/references/logging.md`: Logging sensitive information can lead to data exposure and compliance violations.
- `.claude/skills/golang-security/references/memory-safety.md`: Memory safety vulnerabilities can lead to crashes, data corruption, and security compromises.
- `.claude/skills/golang-security/references/network.md`: Network and web security vulnerabilities can lead to data leakage and unauthorized access.
- `.claude/skills/golang-security/references/secrets.md`: Hardcoded secrets, credentials, and sensitive data in source code is a major security vulnerability.
- `.claude/skills/golang-security/references/third-party.md`: Third-party monitoring and analytics services can inadvertently transmit sensitive user data to external systems.
- `.claude/skills/golang-security/references/threat-modeling.md`: Systematic methodology for identifying and prioritizing security threats in Go applications.

## Stay Updated with Go

Provides resources to stay updated with Golang news, communities and people to follow. Use when seeking Go learning resources, discovering new libraries, finding community channels, or keeping up with Go language changes and releases.

- `.claude/skills/golang-stay-updated/SKILL.md`

## Go Testing Patterns

Go testing patterns including table-driven tests, subtests, benchmarks, fuzzing, and test coverage. Follows TDD methodology with idiomatic Go practices.

- `.claude/skills/golang-testing/SKILL.md`

## Go Troubleshooting Guide

Troubleshoot Golang programs systematically - find and fix the root cause. Use when encountering bugs, crashes, deadlocks, or unexpected behavior in Go code. Covers debugging methodology, common Go pitfalls, test-driven debugging, pprof setup and capture, Delve debugger, race detection, GODEBUG t...

- `.claude/skills/golang-troubleshooting/SKILL.md`
- `.claude/skills/golang-troubleshooting/references/code-review-flags.md`: If you see these in code review, flag them:
- `.claude/skills/golang-troubleshooting/references/common-go-bugs.md`: → See `samber/cc-skills-golang@golang-safety` skill for in-depth nil, slice, and map safety patterns.
- `.claude/skills/golang-troubleshooting/references/compilation.md`
- `.claude/skills/golang-troubleshooting/references/concurrency-debug.md`: **Symptoms:** Memory slowly increasing, goroutine count growing, no obvious CPU spike.
- `.claude/skills/golang-troubleshooting/references/diagnostic-tools.md`: **Output:**
- `.claude/skills/golang-troubleshooting/references/methodology.md`: For any bug, follow this systematic process:
- `.claude/skills/golang-troubleshooting/references/performance-debug.md`: Use pprof CPU profile to capture a 30s sample (see [pprof.md](./pprof.md) for commands), then inspect with `top`, `web`, or `list funcName`.
- `.claude/skills/golang-troubleshooting/references/pprof.md`: Pprof endpoints MUST be protected with basic auth — NEVER expose them publicly. They leak sensitive runtime information (goroutine stacks, memory contents) and can be abused to DoS your service (CPU profiling is expensive). Pprof SHOULD be toggled via a `PPROF_ENABLED` environment variable.
- `.claude/skills/golang-troubleshooting/references/production-debug.md`: When paged for a production issue:
- `.claude/skills/golang-troubleshooting/references/testing-debug.md`: A failing test MUST be written before fixing a bug. Writing a failing test is often the fastest debugging path. It gives you a reproducible, isolated environment.

## Go Development Instructions

- `.claude/skills/golang/SKILL.md`

## Senior Backend

Comprehensive backend development skill for building scalable backend systems using NodeJS, Express, Go, Python, Postgres, GraphQL, REST APIs. Includes API scaffolding, database optimization, security implementation, and performance tuning. Use when designing APIs, optimizing database queries, im...

- `.claude/skills/senior-backend/SKILL.md`
- `.claude/skills/senior-backend/references/api_design_patterns.md`: This reference guide provides comprehensive information for senior backend.
- `.claude/skills/senior-backend/references/backend_security_practices.md`: This reference guide provides comprehensive information for senior backend.
- `.claude/skills/senior-backend/references/database_optimization_guide.md`: This reference guide provides comprehensive information for senior backend.

<!-- autoskills:end -->
